{% extends 'base.html.twig' %}

{% block title %}Edit Setup{% endblock %}

{% block main %}
<div class="main">
    <h1>Edit Setup</h1>

    {{ include('setup/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('setup_index') }}">back to list</a>

    {{ include('setup/_delete_form.html.twig') }}
</div>
{% endblock %}
