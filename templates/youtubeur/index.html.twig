{% extends 'base.html.twig' %}

{% block title %}Youtubeurs{% endblock %}

{% block main %}
<div class="main">
    <div class="container">
        <div class="row">
            {% for youtubeur in youtubeurs %}
            <div class="col-lg-3 col-md-6 mb-4">
                <div class="card h-100">
                    <a href="{{ path('youtubeur_show', {'id': youtubeur.id}) }}"><img class="card-img-top" src={{ asset('images/youtubeurs/'~youtubeur.image) }} alt=""></a>
                    <div class="card-body">
                        <h4 class="card-title">
                        {{ youtubeur.name }}
                        </h4>
                    </div>
                </div>
            </div>
            {% else %}
                <tr>
                    <td colspan="6">Aucun résultat trouvé</td>
                </tr>
            {% endfor %}
        </div>
        <a class ="text-white" href="{{ path('youtubeur_new') }}">Create new</a>
    </div>
</div>
{% endblock %}

